{
  "name": "smocky-bird",
  "version": "1.0.0",
  "private": true,
  "homepage": "/smocky-bird",
  "devDependencies": {
    "react-scripts": "^2.1.5"
  },
  "dependencies": {
    "react": "^16.8.3",
    "react-dom": "^16.8.3",
    "relite": "^1.0.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "deploy": "npm run build && gulp deploy"
  },
  "eslintConfig": {
    "extends": "./node_modules/react-scripts/config/eslint.js",
    "rules": {
      "new-cap": 0
    }
  }
}
